
f: () -> (
    age: std::size_t, 
    name: std::string
    ) = {
    age = 38;
    name = "Stefano";
}

main: () -> int = {
    res := f();
    std::cout << res.name << " is " << std::to_string(res.age) << " years old." << std::endl;
    return 0;
}